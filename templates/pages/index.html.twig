{% extends 'base.html.twig' %}
 {% block content %}
     <div class="main-banner">
         <div class="container">
             <div class="col-lg-12">
                 <div class="top-text header-text">
                     <h2>Debug Gaming</h2>
                     {% for flashSuccess in app.flashes('success') %}
                       <div class="alert alert-success" role="alert">{{ flashSuccess }}</div>
                     {% endfor %}
                     <img class="welcomeImg" src="{{ asset('assets/images/get4net190203516.jpg') }}" alt="">
                 </div>
             </div>
         </div>
     </div>


     <div class="popular-categories">
         <div class="container">
             <div class="row">
                 <div class="col-lg-12">
                     <div class="section-heading">
                         <p>Bienvenue sur Debug Gaming! Ce site a pour vocation de répertorier différents bugs dans les jeux vidéos, et d'indiquer
                         quand il existe une ou plusieurs solutions, d'expliquer ces dernières, ainsi que d'éventuelles corrections suite à des patchs des développeurs.
                         Chacun peut contribuer en ajoutant un ou plusieurs bugs que vous avez expérimenté, ainsi que leurs solutions si vous en connaissez une.
                         On compte donc sur vous pour savoir à quoi s'attendre face aux bugs, voire à les résoudre !</p>
                     </div>
                 </div>
             </div>
         </div>
     </div>


     <div class="recent-listing">
         <div class="container">
             <div class="row">
                 <div class="col-lg-12">
                     <div class="section-heading">
                         <h2>Posts récents</h2>
                     </div>
                 </div>
                 <div class="col-lg-12">
                     <div class="owl-carousel owl-listing">
                         <div class="item">
                             <div class="row">
                                 {% for listBugs in listBugs %}
                                 <div class="col-lg-4">
                                     <div class="listing-item">
                                         <div class="left-image">
                                             <a href="{{ path('bugPage', {'id': listBugs.id}) }}"><img src="{{ asset('assets/images/' ~listBugs.descriptionImgBug ) }}" alt=""></a>
                                         </div>
                                         <div class="right-content align-self-center">
                                             <a href="{{ path('bugPage', {'id': listBugs.id}) }}">
                                                 <h4>{{ listBugs.titleBug }}</h4>
                                             </a>
                                             <h6>{{ listBugs.subtitleBug }}</h6>
                                         </div>
                                     </div>
                                 </div>
                                 {% endfor %}
                             </div>
                         </div>
                     </div>
                 </div>
                 <div class="col-lg-4"></div>
                 <div class="col-lg-4">
                     <div class="nav-links">
                         {% for i in 1..(listBugs.totalItemCount/listBugs.itemNumberPerPage + 1) | round(0, 'floor') %}
                             <a class="page-numbers {{   i == app.request.get('page') ? 'current' : '' }}"
                                href="{{ url(app.request.get('_route'), app.request.query.all() | merge({page: i}) ) }}">{{ i }}</a>
                         {% endfor %}
                     </div>
                 </div>
                 <div class="col-lg-4"></div>
             </div>
         </div>
     </div>
 {% endblock %}